%YAML 1.1
---
version: '>=0.0.0 || @head'
export: |

    project "GainInput"

        kind "StaticLib"

        files "lib/source/**"

        zpm.export [[
            includedirs "lib/include"

            cppdialect "C++11"

            defines "GAINPUT_LIB_BUILD"

            filter "system:windows"
                links "xinput"
            filter "system:linux"
                removefiles "lib/source/gaininput/**Ios.mm"
            filter {}
        ]]
...